# Track only skills and repository documentation

This repository is rooted at `.copilot`, but most files there are local runtime state rather than durable source material. We track only `skills\**`, `.gitattributes`, `.gitignore`, `README.md`, and `docs\adr\**` so skill definitions and repository decisions are backed up without committing settings, sessions, logs, caches, databases, command history, or IDE metadata.
